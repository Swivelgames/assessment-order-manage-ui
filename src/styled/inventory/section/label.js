import styled from 'styled-components';

const InventoryLabel = styled.article`
	color: rgba(0,0,0,0.4);
	font-size: 1.2rem;
	font-weight: 800;
	text-transform: uppercase;
	margin-bottom: 10px;
`;

export default InventoryLabel;
